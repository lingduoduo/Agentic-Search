# Docs sync for #499–#555 — plan

## Task 1: Script the three checks
Link/anchor resolver, tree-vs-`git ls-files`, path existence.
**Verify:** all three run clean or produce an explained finding list.

## Task 2: Repository tree in `docs/architecture.md`
Drift found: `src/cli/` (deleted, and contradicting `docs/cli.md`), the whole
`src/training/` subtree (moved to `src/model/post_training/` by #536–#544),
`internal/context/` (removed in #454), missing `internal/feedback/` and
`internal/memory/`, `servers/connectors/` (gone) and six unlisted `servers/`
directories.
**Verify:** tree matches `git ls-files` at all five levels.

## Task 3: SSE event surface
Code emits seven event types; docs listed four. `claim` (#548) is the headline
feature and appears in none of the three event tables.
**Verify:** every emitted type appears; payload keys read from the code, not guessed.

## Task 4: Session ownership contract
`caller_may_use_session` gates session reads and `/chat/*`; a non-owner gets **404,
not 403**, deliberately, so the response cannot confirm an id exists.
**Verify:** documented in `api-reference.md` with the reason.

## Task 5: Remaining behavioural claims
`AgenticSearchStore` lock (#545); `rl/` → `grpo/` (#542); README's
"background processing" (worker fleet removed) and "GRPO/PPO" (DPO landed in #539,
PPO-with-critic removed in #528).
**Verify:** greps for each old term return nothing.

## Task 6: Contradiction sweep
Cross-doc and intra-doc. Re-grep the OLD wording after every edit, whole file.
**Verify:** no doc still lists the superseded event set.
