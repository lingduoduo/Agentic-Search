{
  "name": "agentic-search",
  "private": true,
  "scripts": {
    "test": "npm --prefix web run test",
    "test:ci": "npm --prefix web run test:ci",
    "web:build": "npm --prefix web run build",
    "web:dev": "npm --prefix web run dev",
    "web:typecheck": "npm --prefix web run typecheck"
  }
}
