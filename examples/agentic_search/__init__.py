"""Supporting modules for the ``examples.run_agentic_search`` CLI.

The CLI itself stays in one file so the three modes read top to bottom. What
lives here is what you do not read to understand that flow: the argparse
declaration, and the opt-in routing behind --intent_index and --model_routing.
"""
